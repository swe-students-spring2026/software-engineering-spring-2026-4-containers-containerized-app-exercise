services:
    mongodb:
      image: mongo:latest
      ports:
        - "27017:27017"
      volumes: 
        - ./mongodb/data:/data/db
    web-app:
      build: ./web-app
      ports: 
        - "5000:5000"
      volumes:
        - ./web-app:/app

